# Offline mode does not require this file: the portable runtime persists application data in
# portable-data/agent-ops-state.json using the local JSON adapter by default.
#
# Only copy this file to database-url.txt when intentionally using the optional MySQL/TiDB path,
# and set PORTABLE_PERSISTENCE=mysql in the host environment before launching.
mysql://user:password@127.0.0.1:3306/agent_ops_desk
